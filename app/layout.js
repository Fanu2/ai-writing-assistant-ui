export const metadata = { title: "AI Writing Assistant", description: "Generate text using OpenAI" };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}